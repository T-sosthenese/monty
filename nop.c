#include "monty.h"

/**
 * nop - used as placeholder of the monty byte code file
 * @stack: double pointer to the stack
 * @line_number: line number containing monty opcode
 */
/* No operation */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
